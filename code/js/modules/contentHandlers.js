define({

  // Handler prototype:
  // function handle<REQUEST.CMD>(args, sender, sendResponse)
  //
  // See util/messaging.js for more details.
  //

  /* redefine
  handleXXX: function(args, sender, sendResponse) {
    sendResponse({});
  }
  */

});
